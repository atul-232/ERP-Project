# Database Connection Configurations
# For local deployment with separate databases (default):
# Leave db.name blank, and the system will auto-create and connect to 'auth_db' and 'erp_db'
db.host=localhost
db.port=3307
db.user=university_erp_user
db.password=User#ERP@portal

# For cloud deployment (e.g., Aiven / Clever Cloud single database):
# Set db.name to your cloud database name. The app will put all tables in this single schema.
# db.name=defaultdb